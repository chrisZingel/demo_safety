<%= form_with(model: organisation) do |form| %>
  <% if organisation.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(organisation.errors.count, "error") %> prohibited this organisation from being saved:</h2>

      <ul>
        <% organisation.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <div class="field">
      <%= form.label :name, "Name of organisation" %><br/>
      <%= form.text_field :name %>
    </div>

    <div class="field">
      <%= form.label :description, "Description of Organisation" %><br/>
      <%= form.text_field :description %>
    </div>

  </div>

  <div class=" my-2">
    <span class="bg-blue-500 py-2 px-4 text-white no-underline">
    <%= form.submit %>
  </span>
  </div>
<% end %>
